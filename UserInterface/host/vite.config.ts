import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import federation from "@originjs/vite-plugin-federation";

export default defineConfig({
  plugins: [
    react(),
    federation({
      name: "host",
      remotes: {
        user_mfe: "http://localhost:4174/assets/remoteEntry.js",
        games_mfe: "http://localhost:4175/assets/remoteEntry.js",
        ratings_mfe: "http://localhost:4176/assets/remoteEntry.js",
      },
      shared: ["react", "react-dom"],
    }),
  ],
  build: {
    target: "esnext",
    minify: false,
    cssCodeSplit: false,
  },
});
